# Manual GitHub Commands to Deploy Netlify Trial Fix

# Navigate to project directory
cd /home/runner/workspace

# Remove git lock if needed
rm -f .git/index.lock

# Add the new Netlify function and routing
git add netlify/functions/admin-create-trial.mjs
git add netlify.toml

# Commit the Netlify trial fix
git commit -m "NETLIFY FIX: Add dedicated trial creation serverless function"

# Push to GitHub using your token format
git push https://tsiemasilo:$PERSONAL_ACCESS_TOKEN_FOREX@github.com/tsiemasilo/forexsignals.git main

# Alternative: Set remote and push
git remote set-url origin https://tsiemasilo:$PERSONAL_ACCESS_TOKEN_FOREX@github.com/tsiemasilo/forexsignals.git
git push origin main